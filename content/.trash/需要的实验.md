### 模型
`rotgcn` 是最终模型
`rot_ctrgcn` 是带有 `rot` 的 `CTRGCN`



### Feeder
`feeder_ntu_rotgcn` 是最终的 `Feeder`



### 参数
参数 `--rot`
为 `0`, 原来的四流融合(先 `rondom_rot` 后的四流)
为 `1`, 使用的是随机旋转模块,五流
为 `2`, 使用的是自适应旋转模块,五流
为 `3`, 原来的四流融合(不使用 `rondom_rot` 后的四流)

