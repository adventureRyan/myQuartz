本文引入了一个自适应视角对齐模块，用于动态学习最适合当前骨架序列的旋转参数。

为实现对每个骨架序列在视角上的个性化对齐，本文引入了一个**自适应视角学习模块**，对每个样本动态学习三个旋转参数，分别表示绕 x、y、z 轴的旋转角度。这种样本级参数化旋转方式，允许模型根据每个序列的姿态和结构自动选择最优观察角度，从而更好地消除视角偏差对时空建模带来的干扰。

与传统的固定视角对齐或整体标准化方法相比，样本级旋转学习具备更强的**灵活性**和**表达能力**，能够在不引入外部规则的前提下，引导模型自主学习最适合每个序列的对齐方式，有效提升视角不变性特征的抽取能力。

