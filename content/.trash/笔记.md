two modules :   
a joint-level module  :
a frame-level module
In addition, we exploit the relationship of joints hierarchically through two modules, i.e., a joint-level module for modeling the correlations of joints in the same frame and a framelevel module for modeling the dependencies of frames by taking the joints in the same frame as a whole.


two joints of the same coordinates but different semantics would deliver very different information.

for joint-level : incorporate the semantics of joint type (e.g., ‘head’, and ‘hip’) to the GCN layers 
for frame-level: incorporate the semantics of temporal frame index to the network.

a Spatial MaxPooling operation

  